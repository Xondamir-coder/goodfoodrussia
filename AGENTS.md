## Image instructions

- Download images into `/public/images/original`
- After downloading all images run `bash transform-images.sh`
- Use `<UiPicture :src='image_name.ext' />` in the template instead of raw `img`
